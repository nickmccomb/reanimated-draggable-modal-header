const ReanimatedDraggableModalHeader = require('./NativeReanimatedDraggableModalHeader').default;

export function multiply(a: number, b: number): number {
  return ReanimatedDraggableModalHeader.multiply(a, b);
}
